[project]
name = "mutap-demo"
version = "0.1.0"
description = "Geração de testes com LLMs e teste de mutação (MuTAP)"
requires-python = ">=3.9,<3.12"
dependencies = [
    "google-genai",
    "mutpy",
    "pytest",
    "python-dotenv",
]
