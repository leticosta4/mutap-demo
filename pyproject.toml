[project]
name = "mutap-demo"
version = "0.1.0"
description = "Geração de testes com LLMs e teste de mutação (MuTAP)"
requires-python = ">=3.10"
dependencies = [
    "google-generativeai",
    "mutpy",
    "pytest",
    "python-dotenv",
]
